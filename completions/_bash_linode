#compdef lin linode linode-cli
# Generated by scripts/generate-bash-completion-stubs.sh; do not edit.
# Lazily loads linode.bash when one of its registered commands first
# requests completion, then delegates the current request to bashcompinit.
autoload -Uz bashcompinit
bashcompinit
source "${ZUSH_BASH_COMPLETIONS_DIR}/linode.bash"

local bash_completion="${_comps[$service]-}"
[[ "$bash_completion" == '_bash_complete '* ]] || return 1
eval "$bash_completion"
